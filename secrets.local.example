# Copy this file to `secrets.local` (which is gitignored) and paste your own key.
#   cp secrets.local.example secrets.local
#
# Used only for research experiments that call an LLM. The orchestrator injects
# these into the coder + judge codex environment at runtime — they never get
# written into the repo, the papers, or any committed artifact.
OPENROUTER_API_KEY=sk-or-v1-REPLACE_ME
